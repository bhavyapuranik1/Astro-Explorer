IMPORTANT:
- Do NOT execute any Git commands.
- Do NOT run git commit, git push, git pull, git reset, git rebase, git gc, git prune, git checkout, git clean, or any destructive Git operation.
- Do NOT modify the .git directory.
- Do NOT execute terminal commands unless I explicitly approve them.
- Only edit source code files.
- If a terminal command is required, explain why and wait for my approval.